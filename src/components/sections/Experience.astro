---
import Section from "../Section.astro"
import { work as work_es } from "@cv"
import { work as work_en } from "@cv_en"
---

<Section id="experience">
  <h2 data-lang-content="es">Experiencia laboral</h2>
  <h2 data-lang-content="en">Work Experience</h2>

  <ul id="work_list" class="work-list" data-lang-content="es">
    {
      work_es.map(
        ({ name, startDate, endDate, position, summary, highlights, url, type, logo }) => {
          const startYear = new Date(startDate).getFullYear()
          const endYear =
            endDate != null ? new Date(endDate).getFullYear() : "Actual"
          const years = `${startYear} - ${endYear}`

          return (
            <li class="work_item">
              <article>
                <header>
                  <div>
                      <a href={url} title={`Ver ${name}`} target="_blank">
                        <span class="work_item_image"><img src={logo} alt={name}></span>
                        <h2>
                          {name} <br/>
                          <span>{position}</span>
                        </h2>
                      </a>
                  </div>
                  <div class="work-info">
                    <time>{years}</time>
                    <p>{type}</p>
                  </div>
                </header>
                <footer>
                  <p>{summary}</p>
                  {highlights && (
                  <ul id="work_highlights">
                    {highlights.map((highlight) => (
                      <li>{highlight}</li>
                    ))}
                  </ul>
                  )}
                </footer>
              </article>
            </li>
          )
        }
      )
    }
  </ul>

  <ul id="work_list_en" class="work-list" data-lang-content="en">
    {
      work_en.map(
        ({ name, startDate, endDate, position, summary, highlights, url, type, logo }) => {
          const startYear = new Date(startDate).getFullYear()
          const endYear =
            endDate != null ? new Date(endDate).getFullYear() : "Present"
          const years = `${startYear} - ${endYear}`

          return (
            <li class="work_item">
              <article>
                <header>
                  <div>
                      <a href={url} title={`View ${name}`} target="_blank">
                        <span class="work_item_image"><img src={logo} alt={name}></span>
                        <h2>
                          {name} <br/>
                          <span>{position}</span>
                        </h2>
                      </a>
                  </div>
                  <div class="work-info">
                    <time>{years}</time>
                    <p>{type}</p>
                  </div>
                </header>
                <footer>
                  <p>{summary}</p>
                  {highlights && (
                  <ul id="work_highlights">
                    {highlights.map((highlight) => (
                      <li>{highlight}</li>
                    ))}
                  </ul>
                  )}
                </footer>
              </article>
            </li>
          )
        }
      )
    }
  </ul>
</Section>